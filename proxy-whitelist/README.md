# ClashMate

**白名单走代理**的 Chrome 代理扩展：只有加入白名单的域名走 Clash 代理，其余全部直连。

## 简单说

- 点击图标一键开关代理
- 「抓取本页接口域名」：自动刷新当前页，10 秒抓取该页全部接口域名，**自动加入白名单**
- 抓取期间全局代理：目标网站不在白名单也能完整打开，抓完自动恢复
- 规则文件 `rules/proxy-whitelist.txt`，编辑保存后重开弹窗即生效

## 安装

`chrome://extensions` → 开发者模式 → 加载已解压的扩展程序 → 选择本目录。
