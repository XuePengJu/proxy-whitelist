# ClashMate 代理白名单
# 命中以下规则的域名/IP 走 Clash 代理，其余全部直连（不走代理）。
# 编辑本文件保存后，重开插件弹窗（或规则管理页）即生效。
#
# 支持格式（每行一条，空行与 # 注释忽略）：
#   baidu.com        -> 域名后缀匹配（baidu.com 及其所有子域）
#   =example.com     -> 精确匹配（仅 example.com 本域）
#   *.qq.com         -> 通配匹配
#   223.5.5.0/24     -> IP 段（CIDR）
#   114.114.114.114  -> 单个 IP
